# AI shell assistant

basic idea of an ai assistant in the shell

Install rust

```sh
cargo build --release
```

run the binary at the target/relesae/dsh

for open ai you need to export OPENAI_KEY as an environment variable
